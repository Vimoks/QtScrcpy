diff --git a/QtScrcpy/ui/videoform.cpp b/QtScrcpy/ui/videoform.cpp
index eb894ed..80d2da0 100644
--- a/QtScrcpy/ui/videoform.cpp
+++ b/QtScrcpy/ui/videoform.cpp
@@ -882,6 +882,17 @@ void VideoForm::keyReleaseEvent(QKeyEvent *event)
     emit device->keyEvent(event, m_frameSize, widgetSize);
 }
 
+void VideoForm::focusOutEvent(QFocusEvent *event)
+{
+    // 宏按键扩展：窗口失焦（如 Alt+Tab）时停止所有宏，
+    // 避免 KeyRelease 丢失导致循环宏停不下来或触点滞留
+    auto device = qsc::IDeviceManage::getInstance().getDevice(m_serial);
+    if (device) {
+        device->stopAllMacros();
+    }
+    QWidget::focusOutEvent(event);
+}
+
 void VideoForm::paintEvent(QPaintEvent *paint)
 {
     Q_UNUSED(paint)
diff --git a/QtScrcpy/ui/videoform.h b/QtScrcpy/ui/videoform.h
index bdcf740..dc803aa 100644
--- a/QtScrcpy/ui/videoform.h
+++ b/QtScrcpy/ui/videoform.h
@@ -62,6 +62,7 @@ protected:
     void wheelEvent(QWheelEvent *event) override;
     void keyPressEvent(QKeyEvent *event) override;
     void keyReleaseEvent(QKeyEvent *event) override;
+    void focusOutEvent(QFocusEvent *event) override;
 
     void paintEvent(QPaintEvent *) override;
     void showEvent(QShowEvent *event) override;
